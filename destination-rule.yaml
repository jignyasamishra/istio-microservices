apiVersion: networking.istio.io/v1alpha3
   kind: DestinationRule
   metadata:
     name: default
     namespace: istio-microservices
   spec:
     host: "*"
     trafficPolicy:
       tls:
         mode: ISTIO_MUTUAL